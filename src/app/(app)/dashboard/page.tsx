export default function DashboardPage() {
  return (
    <section className="stack">
      <h1>Dashboard</h1>
      <p className="muted">
        Fondasi Formo sudah siap — autentikasi, layout, dan database aktif.
        Daftar dokumen serta editor akan hadir di Phase 2.
      </p>
    </section>
  );
}
